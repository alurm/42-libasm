# An implementation of some libc's functions in NASM Linux x86_64

- `read`
- `write`

- `strcmp`
- `strcpy`
- `strdup`
- `strlen`

# Testing

Either run `make test` (provided that all dependencies have been installed) or run `nix build`.
